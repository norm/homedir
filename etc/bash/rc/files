# ~/etc/bash/rc/files
# -*- Mode: Bash; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 2 -*-
#
# useful output when working with files

alias   diff='diff -u'

alias    f.='find .'
alias   fgr='find . | egrep -i'

have ack
if [ 1 = $? ]; then
    alias fgx='ack -ai'
else
    alias fgx='find . -print0 | xargs -0 egrep -i'
fi

alias   ll='ls -la'
alias   ls='ls -F'

alias   mkdir='mkdir -p'

