# $Id$

PortSystem      1.0

name            p5-test-simple
version         1
revision        1
categories      perl
platforms       darwin
maintainers     nomaintainer
fetch {}
checksum {}
extract {}
use_configure       no
build {}
description Placeholder port because now a part of perl5.10.
long_description ${description}
destroot {
    xinstall -d ${destroot}${prefix}/share/doc/${name}
    system "echo ${long_description} > ${destroot}${prefix}/share/doc/${name}/README.txt"
}
archive {}
livecheck.check     none
master_sites
post-destroot {}
universal_variant   no
